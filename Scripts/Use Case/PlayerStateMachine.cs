using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// In StateMachine<PlayerMovement>, change the class to whatever
// you want the context to be.

// Context will be used to access variables and functions
/* public class PlayerStateMachine : StateMachine<PlayerMovement>
{
    // Change this to whatever your context class is called
    private PlayerMovement _playerMovement;
    // Start is called before the first frame update

    public PlayerStateMachine(PlayerMovement context)
    {
        _playerMovement = context;
        _initialState = new PlayerIdleState(_playerMovement);
        _currentState = _initialState;
        
        
    }

} */
